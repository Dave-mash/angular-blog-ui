dist: trusty
sudo: required

language: node_js
node_js:
  - '10'

branches:
  only:
  - master

install:
  - npm install

before_script:
  - npm install -g @angular/cli

script:
  - ng lint
  - npm run test-headless
